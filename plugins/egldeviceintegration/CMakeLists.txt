if(ENABLE_EGLDEVICEINTEGRATION_KMS)
    add_subdirectory(kms)
endif()
if(ENABLE_EGLDEVICEINTEGRATION_X11)
    add_subdirectory(x11)
endif()
if(ENABLE_EGLDEVICEINTEGRATION_BRCM)
    add_subdirectory(brcm)
endif()
if(ENABLE_EGLDEVICEINTEGRATION_MALI)
    add_subdirectory(mali)
endif()
